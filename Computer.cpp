#include "Computer.h"


char Computer::makeMove() {
   /* srand(time(0));
    // Random number
    int num = rand() % 3 + 1;
 
    if(num==1) return 'R';
    if(num==2) return 'P';
    if(num==3) return 'S'; */
    return 'R';
}
